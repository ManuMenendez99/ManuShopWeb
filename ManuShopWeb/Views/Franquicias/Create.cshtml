@model ManuShopWeb.Models.Franquicia

@{
    ViewData["Title"] = "Create";
    int jefe = int.Parse(ViewData["JEFE"].ToString());
}

<h1>Crear</h1>

<h4>Franquicia</h4>
<hr />
<div class="row">
    <div class="col-md-12">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="nombre" class="control-label"></label>
                <input asp-for="nombre" class="form-control" />
                <span asp-validation-for="nombre" class="text-danger"></span>
            </div>
            <div class="form-group" hidden>
                <label asp-for="jefe" hidden class="control-label"></label>
                <input asp-for="jefe" hidden class="form-control" />
                <span asp-validation-for="jefe" hidden value="@jefe" class="text-danger"></span>
            </div>
            <input type="number" hidden name="jefe" value="@jefe" />
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="MisFranquicias" class="btn btn-outline-dark" asp-route-id="@jefe">Volver al listado</a>

</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
